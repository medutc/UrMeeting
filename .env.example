PORT=3000
NODE_ENV=production
SESSION_SECRET=replace-with-a-long-random-string
APP_URL=https://your-app-name.up.railway.app

# Gmail (use an App Password, NOT your real password)
GMAIL_USER=urmeeting1@gmail.com
GMAIL_PASSWORD=your-16-char-gmail-app-password

# Optional SMTP instead of Gmail:
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=
# SMTP_PASS=

# Persistent volume paths (set in Railway/Render dashboard, NOT here in prod)
# DATA_DIR=/data
# UPLOAD_DIR=/data/uploads